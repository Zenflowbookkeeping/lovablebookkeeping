# Serve static assets directly (Cloudflare will also serve them automatically)
# These lines are optional; Cloudflare serves assets in public by default
/*.js   /:splat  200
/*.css  /:splat  200
/*.png  /:splat  200
/*.jpg  /:splat  200
/*.jpeg /:splat  200
/*.svg  /:splat  200
/*.webp /:splat  200
/*.ico  /:splat  200
/*.json /:splat  200
/*.map  /:splat  200

# SPA fallback — only one rule
/*      /index.html  200
